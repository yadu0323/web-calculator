# 🧮 Web Calculator (Flask)

A beginner-friendly **full-stack web calculator** built using **Python (Flask)** with a clean **phone-style UI**.

This project demonstrates how a **frontend communicates with a Python backend using an API**.

---

## 🚀 Features

- Phone-style calculator UI  
- Supports:
  - Addition (+)
  - Subtraction (−)
  - Multiplication (×)
  - Division (÷)
- Backend calculation using **Python**
- Frontend built with **HTML, CSS, JavaScript**
- Real **REST API** using Flask
- Beginner-readable code

---

## 🧠 How It Works (Simple Explanation)

1. User clicks buttons on the calculator (Frontend)
2. JavaScript collects the numbers and operator
3. Data is sent to the Flask API as JSON
4. Python performs the calculation
5. Result is returned to the browser
6. Screen updates with the result
